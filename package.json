{
  "name": "imu-fft",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "dev:frontend": "vite --host 127.0.0.1 --port 1420",
    "build": "npm run build:frontend && cargo build --manifest-path src-tauri/Cargo.toml",
    "build:frontend": "tsc && vite build",
    "build:tauri": "tauri build",
    "test": "cargo test --manifest-path src-tauri/Cargo.toml"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.5.0",
    "@tauri-apps/plugin-process": "^2.3.1",
    "@tauri-apps/plugin-updater": "^2.10.1",
    "echarts": "^6.0.0",
    "three": "^0.176.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.5.0",
    "@types/three": "^0.184.0",
    "typescript": "^5.8.3",
    "vite": "^6.3.5"
  }
}
